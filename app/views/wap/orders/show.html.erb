<% provide(:title, "修改订单信息") %>

销售客户: <%= @order.shop_customer.try(:name) %><br>
结算方式: <%= @order.settlement_type_name %><br>
<!-- 销售员:  -->
商品: 

<%= @order.order_items.each do |order_item| %>
<%= order_item.product.name %>&nbsp;&nbsp;
<%= order_item.quantity %>
<% end %>
<br>

配送方式: <%= @order.dilivery_way_name %><br>

<br><br>

<%=link_to '修改', edit_wap_order_path(@order) %> | 
<%= link_to '取消', cancel_wap_order_path(@order), method: :post, data: {confirm: '确定取消?'} %>